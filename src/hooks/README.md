# `src/hooks`

Custom React hooks compartilhados:

- `usePermission()` — verificação client-side baseada na sessão.
- `useSession()` — wrapper do `next-auth/react`.
- `useDebounce`, `useMediaQuery`, etc.

Hooks devem ser puros (sem efeitos colaterais fora do React) e tipados.
