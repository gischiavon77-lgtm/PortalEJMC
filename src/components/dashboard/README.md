# `components/dashboard`

Componentes específicos do módulo Dashboard:

- Cards de KPIs (membros ativos, projetos em andamento/congelados, faturamento, leads).
- Lista de atividades do mês.
- Grid responsivo do dashboard.

Consome `GET /api/dashboard` e `GET /api/dashboard/activities`.
